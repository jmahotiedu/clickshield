{
  "name": "clickshield",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A Manifest V3 browser extension for conventional ad blocking and click-hijack protection.",
  "scripts": {
    "build": "node --experimental-strip-types tools/build-extension.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "test:unit": "vitest run tests/unit",
    "test:e2e": "node -e \"console.log('E2E suite will be introduced in the browser-testing phase.')\"",
    "verify": "npm run format:check && npm run lint && npm run typecheck && npm run test:unit && npm run build"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.0",
    "@types/node": "^24.0.0",
    "esbuild": "^0.28.1",
    "eslint": "^10.0.0",
    "globals": "^16.3.0",
    "prettier": "^3.6.2",
    "typescript": "5.9.3",
    "typescript-eslint": "^8.65.0",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=22.16.0"
  }
}
